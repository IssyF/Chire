<div class="booking-card">
      <div class="container">
  <h1>Book Your Chef Experience!</h1><br>
   <%# <div class="row justify-content-center"> %>

<div class="form">
<%= simple_form_for [@chef, @booking ] do |f| %>
  <%= f.input :start_date_time, label: 'Date', as: :date %>
  <%= f.input :start_date_time, label: 'Time', as: :time %>
  <%= f.input :duration, label: 'Duration (Hours):', collection: 1..12 %>
  <%= f.submit 'Confirm Booking', class:"btn btn-success" %>
<% end %>
</div>
