<%= simple_form_for [@chef, @booking ] do |f| %>
  <%= f.input :start_date_time, label: 'Start date and time', as: :datetime %>
  <%= f.input :end_date_time, label: 'End date and time', as: :datetime %>
  <%= f.submit 'Confirm Booking', class:"btn btn-success" %>
<% end %>
